# setup a git server (http://www.vogella.com/articles/GitHosting/article.html)
sudo adduser git sudo
#sudo adduser git
#sudo usermod -a -G sudo git
su git
# enter password
cd ~/
mkdir -p ~/.ssh/
cat <your_id_rsa.pub_key_from_computer_that_will_be_loging_in >~/.ssh/authorized_keys

#test for ability to login
ssh -i somekey.pem git@$HOSTNAME
# create a test repo on server
git init --bare example.git

#create test repo on client
mkdir example
cd example
git init
touch README
git add .
git commit -m 'test'
git remote add origin git@$HOSTNAME:example.git
git push origin master
